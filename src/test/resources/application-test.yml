spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cloudstorage_test_db
    username: romarekonshin
    password: '123456'
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

server:
  port: 0
